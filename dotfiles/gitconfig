[user]
  name = Min Huang
  email = min.huang@alumni.usc.edu
  signingkey = "5DB05144"
[color]
  diff = auto
  status = auto
  branch = auto
[core]
  editor = vi
  excludesfile = /home/minhuang/.gitignore
[alias]
  ci = commit
  co = !sh -c 'git checkout $1 && git submodule update && git submodule update' -
  br = branch
  lg = log -p --stat
  st = status
  ar = !git-auto-rm
  su = !git submodule init && git submodule update
  psu = !git pull --stat && git submodule init && git submodule update
  pull = pull --stat
