unbind C-b                      # Allow remapping to C-o
unbind r                        # Allow remapping of reload
unbind %                        # Allow remapping of split horizontally
unbind '"'                      # Allow remapping of split vertically

bind r source-file ~/.tmux.conf # Binds C-o r to reload tmux config
bind | split-window -h          # Binds C-o | to split window horizontally
bind - split-window -v          # Binds C-o - to split window vertically
bind C-o last-window            # Binds C-o C-o to window back and forth
bind -n C-M-j select-pane -D    # Binds C-M-j to switch to bottom pane
bind -n C-M-h select-pane -L    # Binds C-M-h to switch to left pane
bind -n C-M-l select-pane -R    # Binds C-M-l to switch to right pane
bind -n C-M-k select-pane -U    # Binds C-M-k to switch to upper pane

set-option -g automatic-rename on
set-option -g bell-action none
set-option -g bell-on-alert off
set-option -g default-terminal 'screen-256color'
set-option -g message-bg green
set-option -g message-fg black
set-option -g pane-active-border-bg black
set-option -g pane-active-border-fg green
set-option -g pane-border-bg black
set-option -g pane-border-fg white
set-option -g prefix C-o
set-option -g set-clipboard on
set-option -g set-titles on
set-option -g set-titles-string '#H:#S.#I.#P #W #T'
set-option -g status-attr none
set-option -g status-bg black
set-option -g status-fg colour245
set-option -g status-justify left
set-option -g status-keys vi
set-option -g status-left '#[fg=green]['
set-option -g status-left-length 50
set-option -g status-right '#[fg=green]] [#[fg=blue]#H#[fg=green]:#[fg=white]#S#[fg=green]]'
set-option -g visual-activity off
set-option -g visual-bell off
set-option -g window-status-activity-attr none
set-option -g window-status-activity-bg black
set-option -g window-status-activity-fg white

set -g mouse on # Enable mouse based pane switching

setw -g aggressive-resize on
setw -g mode-keys vi
setw -g monitor-activity on
setw -g window-status-current-fg green
setw -g window-status-bg black
